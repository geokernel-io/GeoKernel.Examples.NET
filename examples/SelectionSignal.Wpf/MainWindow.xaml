<Window x:Class="GeoKernel.SelectionSignal.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:gk="clr-namespace:GeoKernel.NET.Wpf.Controls;assembly=GeoKernel.NET.Wpf.Controls"
        Title="SelectionSignal"
        Width="1200"
        Height="800"
        MinWidth="900"
        MinHeight="600"
        WindowStartupLocation="CenterScreen"
        Icon="Images/GeoKernelAppIcon.ico"
        Loaded="Window_Loaded">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="32" />
            <RowDefinition Height="*" />
            <RowDefinition Height="24" />
        </Grid.RowDefinitions>

        <ToolBarTray Grid.Row="0" Background="#EFEFEF">
            <ToolBar Height="32"
                     Background="#EFEFEF"
                     BorderThickness="0"
                     Padding="0"
                     Band="0"
                     BandIndex="0">
                <ToggleButton x:Name="addButton"
                              Padding="10,0"
                              IsChecked="True"
                              Click="Add_Click">Add Select</ToggleButton>
                <ToggleButton x:Name="toggleButton"
                              Padding="10,0"
                              Click="Toggle_Click">Toggle Select</ToggleButton>
                <ToggleButton x:Name="panButton"
                              Padding="10,0"
                              Click="Pan_Click">Pan</ToggleButton>
                <Separator />
                <Button Padding="10,0" Click="Clear_Click">Clear Selection</Button>
                <Button Padding="10,0" Click="FullExtent_Click">Full Extent</Button>
                <TextBlock x:Name="toolStateText"
                           Margin="12,0,0,0"
                           VerticalAlignment="Center"
                           FontWeight="SemiBold"
                           Text="Selected: 0 | Signal: SelectionChanged" />
            </ToolBar>
        </ToolBarTray>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="346" />
            </Grid.ColumnDefinitions>

            <gk:GeoKernelViewerControl x:Name="viewerControl"
                                       Grid.Column="0"
                                       MouseLeftButtonUp="ViewerControl_MouseLeftButtonUp" />

            <DataGrid x:Name="detailsGrid"
                      Grid.Column="1"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      IsReadOnly="True"
                      SelectionMode="Single"
                      SelectionUnit="FullRow">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="#" Binding="{Binding Number}" Width="42" />
                    <DataGridTextColumn Header="Event" Binding="{Binding EventName}" Width="*" />
                    <DataGridTextColumn Header="Selected" Binding="{Binding SelectedCount}" Width="76" />
                    <DataGridTextColumn Header="Message" Binding="{Binding Message}" Width="170" />
                </DataGrid.Columns>
            </DataGrid>
        </Grid>

        <Border Grid.Row="2" Background="#F4F4F4" BorderBrush="#D0D0D0" BorderThickness="0,1,0,0">
            <TextBlock x:Name="statusText"
                       VerticalAlignment="Center"
                       Margin="4,0"
                       Text="Ready" />
        </Border>
    </Grid>
</Window>

