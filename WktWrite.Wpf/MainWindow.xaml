<Window x:Class="GeoKernel.WktWrite.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:gk="clr-namespace:GeoKernel.NET.Wpf.Controls;assembly=GeoKernel.NET.Wpf.Controls"
        Title="WktWrite"
        Width="1120"
        Height="720"
        MinWidth="840"
        MinHeight="520"
        WindowStartupLocation="CenterScreen"
        Icon="Images/GeoKernelAppIcon.ico"
        Loaded="Window_Loaded">
    <DockPanel>
        <StackPanel DockPanel.Dock="Top"
                    Orientation="Horizontal"
                    Margin="6,4">
            <TextBlock Text="Geometry:"
                       VerticalAlignment="Center"
                       Margin="0,0,8,0" />
            <ComboBox x:Name="geometryComboBox"
                      Width="130"
                      SelectionChanged="GeometryComboBox_SelectionChanged">
                <ComboBoxItem Content="Point"
                              IsSelected="True" />
                <ComboBoxItem Content="Polyline" />
                <ComboBoxItem Content="Polygon" />
            </ComboBox>
            <Button Content="Clear"
                    Margin="10,0,0,0"
                    Padding="12,2"
                    Click="Clear_Click" />
            <TextBlock x:Name="hintText"
                       VerticalAlignment="Center"
                       Margin="10,0,0,0" />
        </StackPanel>

        <StatusBar DockPanel.Dock="Bottom">
            <TextBlock x:Name="statusText" />
        </StatusBar>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="4" />
                <ColumnDefinition Width="416" />
            </Grid.ColumnDefinitions>

            <gk:GeoKernelViewerControl x:Name="viewerControl"
                                       Grid.Column="0" />

            <GridSplitter Grid.Column="1"
                          Width="4"
                          HorizontalAlignment="Stretch"
                          VerticalAlignment="Stretch"
                          ResizeDirection="Columns"
                          ResizeBehavior="PreviousAndNext" />

            <TextBox x:Name="detailsTextBox"
                     Grid.Column="2"
                     IsReadOnly="True"
                     TextWrapping="Wrap"
                     AcceptsReturn="True"
                     VerticalScrollBarVisibility="Auto" />
        </Grid>
    </DockPanel>
</Window>
